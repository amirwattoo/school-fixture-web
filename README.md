# Proxy Management

Next.js client for the multi-school Proxy Management API. Configure `NEXT_PUBLIC_API_URL` to the deployed API `/api/v1` base URL. Password reset links are generated by the API from its `FRONTEND_URL`. Timetable administrators can upload PDF or CSV files from **Timetable Import**, review school-scoped mappings, and explicitly confirm replacement.
